import { test } from '@playwright/test'

// Dashboard zaten auth.setup.ts içinde doğrulanıyor (giriş sonrası başlık kontrolü).
// Bu dosya boş bırakıldı; gerekirse burada ek smoke testleri yazılabilir.
